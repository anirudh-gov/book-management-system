<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Management</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            margin: 20px;
            background-color: #f8f9fa;
        }
        h1 {
            margin-bottom: 20px;
            color: #343a40;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .btn-add {
            display: block;
            width: 100%;
            margin-top: 15px;
        }
        .table {
            margin-top: 20px;
        }
        th, td {
            text-align: center;
        }
        .btn-delete {
            width: 100%;
        }
        .container {
            max-width: 900px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Book Management</h1>
        <div class="row">
            <form method="post" action="/books" class="col-md-12">
                <div class="form-group col-md-6">
                    <label for="title">Book Title</label>
                    <input type="text" placeholder="Enter Title" class="form-control" id="title" name="title" required>
                </div>
                <div class="form-group col-md-6">
                    <label for="author">Book Author</label>
                    <input type="text" placeholder="Author name" class="form-control" id="author" name="author" required>
                </div>
                <div class="form-group col-md-6">
                    <label for="genre">Genre</label>
                    <input type="text" placeholder="Genre" class="form-control" id="genre" name="genre" required>
                </div>
                <div class="form-group col-md-6">
                    <label for="price">Price</label>
                    <input type="number" placeholder="Enter Price" class="form-control" id="price" name="price" required>
                </div>
                <button class="btn btn-outline-success btn-add">Add Book</button>
            </form>
        </div>
        <br><br>
        <h3>Book List</h3>
        <table class="table table-light table-hover">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>Genre</th>
                    <th>Price</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for (book of books) { %>
                <tr>
                    <td><%= book.title %></td>
                    <td><%= book.author %></td>
                    <td><%= book.genre %></td>
                    <td><%= book.price %></td>
                    <td>
                        <form action="/books/<%= book._id %>?_method=DELETE" method="post">
                            <button class="btn btn-outline-danger btn-delete">Delete</button>
                        </form>
                    </td>
                </tr>
                <% } %>
            </tbody>
        </table>
        <br><br>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
